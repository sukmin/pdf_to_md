[package]
name = "pdf_to_md"
version = "1.0.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
pdf-extract = "0.7"
anyhow = "1.0"

[profile.release]
lto = true
codegen-units = 1
